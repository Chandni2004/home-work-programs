#include <stdio.h>
int main()
{
    long int term=9,sum=0;
    int n;
    printf("Enter the value of n: ");
    scanf("%d",&n);
    for(int cnt=1;cnt<=n;cnt++)
    {
        sum=sum+term;
        term=(term*10) + 9;
    }
    printf("%ld",sum);
}
