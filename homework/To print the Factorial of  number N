#include <stdio.h>
int main()
{
	int n,i;
	int fact=1;
	printf("Enter the number to get Factorial: ");
	scanf("%d",&n);
	for(i=n;i>=1; i--) {
		fact = fact * i;
	}
	printf("Factorial is %d", fact);
}
