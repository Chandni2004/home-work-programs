#include <stdio.h>
int main()
{
    int n,i,a,r;
    printf("Enter the number: ");
    scanf("%d",&n);
    for(i=15;i>=0;i--)
    {
        a=1<<i;
        r=n&a;
        if(r==0)
        printf("0");
        else
        printf("1");
    }
    return 0;
}
